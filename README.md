# Skills Hub Registry

Skills live here as directories. Each skill directory contains a `SKILL.md` with frontmatter (`name`, `description`).

The Skills Hub web app reads from this repo via the GitHub API. Install any skill with:

```
npx skills-hub pull <name>
```
